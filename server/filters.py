# import django_filters
# from .models import UserProfile, TrainerProfile

# class RoleFilter(django_filters.FilterSet):
#     class Meta:
#         model = UserProfile  # Filter based on UserProfile model
#         fields = ['is_trainer']

# class TrainerRoleFilter(django_filters.FilterSet):
#     class Meta:
#         model = TrainerProfile  # Filter based on TrainerProfile model
#         fields = ['is_trainer']
